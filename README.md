# BikransOfficialSite

Bikrans official website — React (Vite) frontend with Node.js/Express backend and MySQL.

## Setup

- `npm install`
- Copy `.env.example` to `.env` and set DB credentials
- `npm run migrate` then `npm run seed`
- `npm run server` (backend) and `npm run dev` (frontend)
